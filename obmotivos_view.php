<?php
// This script and data application were generated by AppGini 5.97
// Download AppGini for free from https://bigprof.com/appgini/download/

	$currDir = dirname(__FILE__);
	include_once("{$currDir}/lib.php");
	@include_once("{$currDir}/hooks/obmotivos.php");
	include_once("{$currDir}/obmotivos_dml.php");

	// mm: can the current member access this page?
	$perm = getTablePermissions('obmotivos');
	if(!$perm['access']) {
		echo error_message($Translation['tableAccessDenied']);
		exit;
	}

	$x = new DataList;
	$x->TableName = 'obmotivos';

	// Fields that can be displayed in the table view
	$x->QueryFieldsTV = [
		"IF(    CHAR_LENGTH(`motivos1`.`motivo`), CONCAT_WS('',   `motivos1`.`motivo`), '') /* Motivo */" => "motivo",
		"`obmotivos`.`observacion`" => "observacion",
	];
	// mapping incoming sort by requests to actual query fields
	$x->SortFields = [
		1 => '`motivos1`.`motivo`',
		2 => 2,
	];

	// Fields that can be displayed in the csv file
	$x->QueryFieldsCSV = [
		"IF(    CHAR_LENGTH(`motivos1`.`motivo`), CONCAT_WS('',   `motivos1`.`motivo`), '') /* Motivo */" => "motivo",
		"`obmotivos`.`observacion`" => "observacion",
	];
	// Fields that can be filtered
	$x->QueryFieldsFilters = [
		"IF(    CHAR_LENGTH(`motivos1`.`motivo`), CONCAT_WS('',   `motivos1`.`motivo`), '') /* Motivo */" => "Motivo",
		"`obmotivos`.`observacion`" => "Observacion",
	];

	// Fields that can be quick searched
	$x->QueryFieldsQS = [
		"IF(    CHAR_LENGTH(`motivos1`.`motivo`), CONCAT_WS('',   `motivos1`.`motivo`), '') /* Motivo */" => "motivo",
		"`obmotivos`.`observacion`" => "observacion",
	];

	// Lookup fields that can be used as filterers
	$x->filterers = ['motivo' => 'Motivo', ];

	$x->QueryFrom = "`obmotivos` LEFT JOIN `motivos` as motivos1 ON `motivos1`.`motivo`=`obmotivos`.`motivo` ";
	$x->QueryWhere = '';
	$x->QueryOrder = '';

	$x->AllowSelection = 1;
	$x->HideTableView = ($perm['view'] == 0 ? 1 : 0);
	$x->AllowDelete = $perm['delete'];
	$x->AllowMassDelete = (getLoggedAdmin() !== false);
	$x->AllowInsert = $perm['insert'];
	$x->AllowUpdate = $perm['edit'];
	$x->SeparateDV = 1;
	$x->AllowDeleteOfParents = 0;
	$x->AllowFilters = 1;
	$x->AllowSavingFilters = (getLoggedAdmin() !== false);
	$x->AllowSorting = 1;
	$x->AllowNavigation = 1;
	$x->AllowPrinting = 1;
	$x->AllowPrintingDV = (getLoggedAdmin() !== false);
	$x->AllowCSV = 1;
	$x->RecordsPerPage = 10;
	$x->QuickSearch = 1;
	$x->QuickSearchText = $Translation['quick search'];
	$x->ScriptFileName = 'obmotivos_view.php';
	$x->RedirectAfterInsert = 'obmotivos_view.php?SelectedID=#ID#';
	$x->TableTitle = 'Observaciones a Motivo';
	$x->TableIcon = 'resources/table_icons/google_custom_search.png';
	$x->PrimaryKey = '`obmotivos`.`observacion`';

	$x->ColWidth = [150, 150, ];
	$x->ColCaption = ['Motivo', 'Observacion', ];
	$x->ColFieldName = ['motivo', 'observacion', ];
	$x->ColNumber  = [1, 2, ];

	// template paths below are based on the app main directory
	$x->Template = 'templates/obmotivos_templateTV.html';
	$x->SelectedTemplate = 'templates/obmotivos_templateTVS.html';
	$x->TemplateDV = 'templates/obmotivos_templateDV.html';
	$x->TemplateDVP = 'templates/obmotivos_templateDVP.html';

	$x->ShowTableHeader = 1;
	$x->TVClasses = "";
	$x->DVClasses = "";
	$x->HasCalculatedFields = false;
	$x->AllowConsoleLog = false;
	$x->AllowDVNavigation = true;

	// hook: obmotivos_init
	$render = true;
	if(function_exists('obmotivos_init')) {
		$args = [];
		$render = obmotivos_init($x, getMemberInfo(), $args);
	}

	if($render) $x->Render();

	// hook: obmotivos_header
	$headerCode = '';
	if(function_exists('obmotivos_header')) {
		$args = [];
		$headerCode = obmotivos_header($x->ContentType, getMemberInfo(), $args);
	}

	if(!$headerCode) {
		include_once("{$currDir}/header.php"); 
	} else {
		ob_start();
		include_once("{$currDir}/header.php");
		echo str_replace('<%%HEADER%%>', ob_get_clean(), $headerCode);
	}

	echo $x->HTML;

	// hook: obmotivos_footer
	$footerCode = '';
	if(function_exists('obmotivos_footer')) {
		$args = [];
		$footerCode = obmotivos_footer($x->ContentType, getMemberInfo(), $args);
	}

	if(!$footerCode) {
		include_once("{$currDir}/footer.php"); 
	} else {
		ob_start();
		include_once("{$currDir}/footer.php");
		echo str_replace('<%%FOOTER%%>', ob_get_clean(), $footerCode);
	}
