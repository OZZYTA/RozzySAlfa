<?php
// This script and data application were generated by AppGini 5.97
// Download AppGini for free from https://bigprof.com/appgini/download/

	$currDir = dirname(__FILE__);
	include_once("{$currDir}/lib.php");
	@include_once("{$currDir}/hooks/actascierre.php");
	include_once("{$currDir}/actascierre_dml.php");

	// mm: can the current member access this page?
	$perm = getTablePermissions('actascierre');
	if(!$perm['access']) {
		echo error_message($Translation['tableAccessDenied']);
		exit;
	}

	$x = new DataList;
	$x->TableName = 'actascierre';

	// Fields that can be displayed in the table view
	$x->QueryFieldsTV = [
		"`actascierre`.`id`" => "id",
		"`actascierre`.`numero`" => "numero",
	];
	// mapping incoming sort by requests to actual query fields
	$x->SortFields = [
		1 => '`actascierre`.`id`',
		2 => 2,
	];

	// Fields that can be displayed in the csv file
	$x->QueryFieldsCSV = [
		"`actascierre`.`id`" => "id",
		"`actascierre`.`numero`" => "numero",
	];
	// Fields that can be filtered
	$x->QueryFieldsFilters = [
		"`actascierre`.`id`" => "ID",
		"`actascierre`.`numero`" => "Numero",
	];

	// Fields that can be quick searched
	$x->QueryFieldsQS = [
		"`actascierre`.`id`" => "id",
		"`actascierre`.`numero`" => "numero",
	];

	// Lookup fields that can be used as filterers
	$x->filterers = [];

	$x->QueryFrom = "`actascierre` ";
	$x->QueryWhere = '';
	$x->QueryOrder = '';

	$x->AllowSelection = 1;
	$x->HideTableView = ($perm['view'] == 0 ? 1 : 0);
	$x->AllowDelete = $perm['delete'];
	$x->AllowMassDelete = (getLoggedAdmin() !== false);
	$x->AllowInsert = $perm['insert'];
	$x->AllowUpdate = $perm['edit'];
	$x->SeparateDV = 1;
	$x->AllowDeleteOfParents = 0;
	$x->AllowFilters = 1;
	$x->AllowSavingFilters = (getLoggedAdmin() !== false);
	$x->AllowSorting = 1;
	$x->AllowNavigation = 1;
	$x->AllowPrinting = 1;
	$x->AllowPrintingDV = 1;
	$x->AllowCSV = 1;
	$x->RecordsPerPage = 10;
	$x->QuickSearch = 1;
	$x->QuickSearchText = $Translation['quick search'];
	$x->ScriptFileName = 'actascierre_view.php';
	$x->RedirectAfterInsert = 'actascierre_view.php?SelectedID=#ID#';
	$x->TableTitle = 'Actascierre';
	$x->TableIcon = 'table.gif';
	$x->PrimaryKey = '`actascierre`.`id`';

	$x->ColWidth = [150, ];
	$x->ColCaption = ['Numero', ];
	$x->ColFieldName = ['numero', ];
	$x->ColNumber  = [2, ];

	// template paths below are based on the app main directory
	$x->Template = 'templates/actascierre_templateTV.html';
	$x->SelectedTemplate = 'templates/actascierre_templateTVS.html';
	$x->TemplateDV = 'templates/actascierre_templateDV.html';
	$x->TemplateDVP = 'templates/actascierre_templateDVP.html';

	$x->ShowTableHeader = 1;
	$x->TVClasses = "";
	$x->DVClasses = "";
	$x->HasCalculatedFields = false;
	$x->AllowConsoleLog = false;
	$x->AllowDVNavigation = true;

	// hook: actascierre_init
	$render = true;
	if(function_exists('actascierre_init')) {
		$args = [];
		$render = actascierre_init($x, getMemberInfo(), $args);
	}

	if($render) $x->Render();

	// hook: actascierre_header
	$headerCode = '';
	if(function_exists('actascierre_header')) {
		$args = [];
		$headerCode = actascierre_header($x->ContentType, getMemberInfo(), $args);
	}

	if(!$headerCode) {
		include_once("{$currDir}/header.php"); 
	} else {
		ob_start();
		include_once("{$currDir}/header.php");
		echo str_replace('<%%HEADER%%>', ob_get_clean(), $headerCode);
	}

	echo $x->HTML;

	// hook: actascierre_footer
	$footerCode = '';
	if(function_exists('actascierre_footer')) {
		$args = [];
		$footerCode = actascierre_footer($x->ContentType, getMemberInfo(), $args);
	}

	if(!$footerCode) {
		include_once("{$currDir}/footer.php"); 
	} else {
		ob_start();
		include_once("{$currDir}/footer.php");
		echo str_replace('<%%FOOTER%%>', ob_get_clean(), $footerCode);
	}
