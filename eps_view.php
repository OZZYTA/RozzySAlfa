<?php
// This script and data application were generated by AppGini 5.97
// Download AppGini for free from https://bigprof.com/appgini/download/

	$currDir = dirname(__FILE__);
	include_once("{$currDir}/lib.php");
	@include_once("{$currDir}/hooks/eps.php");
	include_once("{$currDir}/eps_dml.php");

	// mm: can the current member access this page?
	$perm = getTablePermissions('eps');
	if(!$perm['access']) {
		echo error_message($Translation['tableAccessDenied']);
		exit;
	}

	$x = new DataList;
	$x->TableName = 'eps';

	// Fields that can be displayed in the table view
	$x->QueryFieldsTV = [
		"`eps`.`razonsocial`" => "razonsocial",
		"IF(    CHAR_LENGTH(`regimenes1`.`nombre`), CONCAT_WS('',   `regimenes1`.`nombre`), '') /* Regimen */" => "regimen",
		"`eps`.`gerente`" => "gerente",
		"`eps`.`telefono`" => "telefono",
		"`eps`.`correo`" => "correo",
		"`eps`.`correo2`" => "correo2",
		"`eps`.`correo3`" => "correo3",
	];
	// mapping incoming sort by requests to actual query fields
	$x->SortFields = [
		1 => 1,
		2 => '`regimenes1`.`nombre`',
		3 => 3,
		4 => 4,
		5 => 5,
		6 => 6,
		7 => 7,
	];

	// Fields that can be displayed in the csv file
	$x->QueryFieldsCSV = [
		"`eps`.`razonsocial`" => "razonsocial",
		"IF(    CHAR_LENGTH(`regimenes1`.`nombre`), CONCAT_WS('',   `regimenes1`.`nombre`), '') /* Regimen */" => "regimen",
		"`eps`.`gerente`" => "gerente",
		"`eps`.`telefono`" => "telefono",
		"`eps`.`correo`" => "correo",
		"`eps`.`correo2`" => "correo2",
		"`eps`.`correo3`" => "correo3",
	];
	// Fields that can be filtered
	$x->QueryFieldsFilters = [
		"`eps`.`razonsocial`" => "Nombre",
		"IF(    CHAR_LENGTH(`regimenes1`.`nombre`), CONCAT_WS('',   `regimenes1`.`nombre`), '') /* Regimen */" => "Regimen",
		"`eps`.`gerente`" => "Gerente",
		"`eps`.`telefono`" => "Telefono",
		"`eps`.`correo`" => "Correo",
		"`eps`.`correo2`" => "CC 1",
		"`eps`.`correo3`" => "CC 2",
	];

	// Fields that can be quick searched
	$x->QueryFieldsQS = [
		"`eps`.`razonsocial`" => "razonsocial",
		"IF(    CHAR_LENGTH(`regimenes1`.`nombre`), CONCAT_WS('',   `regimenes1`.`nombre`), '') /* Regimen */" => "regimen",
		"`eps`.`gerente`" => "gerente",
		"`eps`.`telefono`" => "telefono",
		"`eps`.`correo`" => "correo",
		"`eps`.`correo2`" => "correo2",
		"`eps`.`correo3`" => "correo3",
	];

	// Lookup fields that can be used as filterers
	$x->filterers = ['regimen' => 'Regimen', ];

	$x->QueryFrom = "`eps` LEFT JOIN `regimenes` as regimenes1 ON `regimenes1`.`nombre`=`eps`.`regimen` ";
	$x->QueryWhere = '';
	$x->QueryOrder = '';

	$x->AllowSelection = 1;
	$x->HideTableView = ($perm['view'] == 0 ? 1 : 0);
	$x->AllowDelete = $perm['delete'];
	$x->AllowMassDelete = (getLoggedAdmin() !== false);
	$x->AllowInsert = $perm['insert'];
	$x->AllowUpdate = $perm['edit'];
	$x->SeparateDV = 1;
	$x->AllowDeleteOfParents = 0;
	$x->AllowFilters = 1;
	$x->AllowSavingFilters = (getLoggedAdmin() !== false);
	$x->AllowSorting = 1;
	$x->AllowNavigation = 1;
	$x->AllowPrinting = 1;
	$x->AllowPrintingDV = (getLoggedAdmin() !== false);
	$x->AllowCSV = 1;
	$x->RecordsPerPage = 10;
	$x->QuickSearch = 1;
	$x->QuickSearchText = $Translation['quick search'];
	$x->ScriptFileName = 'eps_view.php';
	$x->RedirectAfterInsert = 'eps_view.php?SelectedID=#ID#';
	$x->TableTitle = 'Entidades';
	$x->TableIcon = 'resources/table_icons/building.png';
	$x->PrimaryKey = '`eps`.`razonsocial`';

	$x->ColWidth = [150, 150, 150, 150, 150, 150, 150, ];
	$x->ColCaption = ['Nombre', 'Regimen', 'Gerente', 'Telefono', 'Correo', 'CC 1', 'CC 2', ];
	$x->ColFieldName = ['razonsocial', 'regimen', 'gerente', 'telefono', 'correo', 'correo2', 'correo3', ];
	$x->ColNumber  = [1, 2, 3, 4, 5, 6, 7, ];

	// template paths below are based on the app main directory
	$x->Template = 'templates/eps_templateTV.html';
	$x->SelectedTemplate = 'templates/eps_templateTVS.html';
	$x->TemplateDV = 'templates/eps_templateDV.html';
	$x->TemplateDVP = 'templates/eps_templateDVP.html';

	$x->ShowTableHeader = 1;
	$x->TVClasses = "";
	$x->DVClasses = "";
	$x->HasCalculatedFields = false;
	$x->AllowConsoleLog = false;
	$x->AllowDVNavigation = true;

	// hook: eps_init
	$render = true;
	if(function_exists('eps_init')) {
		$args = [];
		$render = eps_init($x, getMemberInfo(), $args);
	}

	if($render) $x->Render();

	// hook: eps_header
	$headerCode = '';
	if(function_exists('eps_header')) {
		$args = [];
		$headerCode = eps_header($x->ContentType, getMemberInfo(), $args);
	}

	if(!$headerCode) {
		include_once("{$currDir}/header.php"); 
	} else {
		ob_start();
		include_once("{$currDir}/header.php");
		echo str_replace('<%%HEADER%%>', ob_get_clean(), $headerCode);
	}

	echo $x->HTML;

	// hook: eps_footer
	$footerCode = '';
	if(function_exists('eps_footer')) {
		$args = [];
		$footerCode = eps_footer($x->ContentType, getMemberInfo(), $args);
	}

	if(!$footerCode) {
		include_once("{$currDir}/footer.php"); 
	} else {
		ob_start();
		include_once("{$currDir}/footer.php");
		echo str_replace('<%%FOOTER%%>', ob_get_clean(), $footerCode);
	}
